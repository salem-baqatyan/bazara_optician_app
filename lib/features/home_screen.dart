import 'package:flutter/material.dart';
import 'package:optician_app/core/compent/root.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Root();
  }
}
